version: "3"

services:
  docker:
    build:
      context: ./examples
      dockerfile: ./docker.dockerfile
    volumes:
      - $PWD/scripts:/home/conf.json
      - $PWD/conf.json:/home/conf.json
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 7770:3000

